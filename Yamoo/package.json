{
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "node-sass --output-style expanded sass/main.scss css/modules/main.css",
    "watch": "npm run sass -- -w --source-map ./map",
    "build": "node-sass --output-style compressed css/modules/grid.css css/modules/grid.min.css"
  },
  "devDependencies": {
    "node-sass": "^4.5.3"
   }
}
